version: "3"
services:
  gulp-development:
    build: "."
    image: "mws-builder"
    environment: 
      MODE: "development"
      REMOTE_DB_BASE_URL: "http://localhost:1337"
    ports:
      - "3000:3000"
    volumes:
      - ".:/app"
    command: "npm run dev"